version: 2

models:
  - name: dim_customers
    description: One row per customer with customer profile attributes and lifetime order value rollups.
    config:
      access: public
      contract:
        enforced: true
    semantic_model:
      enabled: true
    agg_time_dimension: first_order_date

    columns:
      - name: customer_id
        description: Primary key for the customer dimension.
        data_type: int
        constraints:
          - type: not_null
        data_tests:
          - not_null
          - unique
        entity:
          name: customer
          type: primary

      - name: first_name
        description: Customer first name.
        data_type: varchar
        dimension:
          type: categorical

      - name: last_name
        description: Customer last name.
        data_type: varchar
        dimension:
          type: categorical

      - name: first_order_date
        description: Date of the customer's first order.
        data_type: date
        granularity: day
        dimension:
          type: time

      - name: most_recent_order_date
        description: Date of the customer's most recent order.
        data_type: date
        granularity: day
        dimension:
          type: time

      - name: number_of_orders
        description: Total number of orders placed by the customer.
        data_type: number(18,0)

      - name: average_monthly_orders
        description: Average monthly orders across the inclusive month span from first order date to most recent order date.
        data_type: number(18,2)

      - name: lifetime_value
        description: Total lifetime revenue attributed to the customer.
        data_type: number(38,6)

    metrics:
      - name: order_count
        type: simple
        description: Total number of orders.
        label: Customer Order Count

        agg: sum
        expr: number_of_orders

      - name: lifetime_revenue
        type: simple
        description: Total lifetime revenue attributed to this customer
        label: Lifetime Revenue
        agg: sum
        expr: lifetime_value
